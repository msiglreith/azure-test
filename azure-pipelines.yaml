jobs:
- template: job-fmt.yaml@templates
  parameters:
    name: rustfmt

resources:
  repositories:
    - repository: templates
      type: github
      name: norse-rs/azure-ci
      endpoint: msiglreith
